#include<iostream>
#include<vector>
using namespace std;
string a[1000];
int main(){
    freopen("input.txt","r",stdin);
    string s;
    int num = 0;
    while(cin >> s){
        a[num++] = s;
    }
    cout<<"采样或停止(采样S -- 停止E):"<<endl;
    while()
    return 0;
}